<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
 
    <link rel="stylesheet" href="style.css" type="text/css">
    
    <!--Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"> 

    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script> 

    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

 <style>
    body {
        background-image: url("<%= data.img.background %>");
        background-position: absolute;
    } 
</style>

</head>
<body >
        <!-- Navigation Bar -->
        <nav id="nav" class="navbar navbar-inverse">
            <div class="container-fluid">
            <ul class="nav navbar-nav">
                <% for(var i=0; i < data.nav.length ; i++){ %>
                 <li><a href="<%= data.nav[i].link %>"><%= data.nav[i].name %></a></li>
                 <% } %>
           </ul>
           </div>
        </nav>

        <div id="home">
             <h2 style = "<%= data.font.margin + data.font.size + data.font.family %>">
                <b><center> Wel<font style = "<%= data.font.color %>">come</font> To 
                <font style = "<%= data.font.color %>">Adappt</font> World</center></b></h2>

             <img id="img" style ="<%= data.img.margin_l + data.img.margin_t + data.img.height + data.img.width + data.img.opacity %>" 
                src="<%= data.img.logo %>" />
        </div>

</body>
</html>